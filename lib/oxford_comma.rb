def oxford_comma(array)
  if array.length = 1
    array
  elsif array.length = 2
    array.join(" and ")
  else
    small_array == array[0..arrayl.length - 2]
    output = small_array.join(", ") + " and #{array.last}"

  end
end
